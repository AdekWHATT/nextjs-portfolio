{
  "private": true,
  "homepage": "https://adekwhatt.github.io/nextjs-portfolio/",
  "scripts": {
    "dev": "next",
    "build": "next build && next export",
    "deploy": "next build && echo '' > out/.nojekyll && git add out/ && git commit -m 'Deploy' && git subtree push --prefix out origin gh-pages"  },
  "dependencies": {
    "gh-pages": "^5.0.0",
    "next": "latest",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "react-slick": "^0.29.0",
    "slick-carousel": "^1.8.1"
  }
}
